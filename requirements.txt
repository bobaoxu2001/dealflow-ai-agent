# --- Web / API ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# --- Database ---
SQLAlchemy>=2.0,<3.0
pgvector>=0.2.5,<1.0
psycopg2-binary>=2.9,<3.0

# --- Agent orchestration ---
langgraph>=0.2.0,<1.0
langchain>=0.3.0,<1.0
langchain-core>=0.3.0,<1.0

# --- Data ---
pandas>=2.1,<3.0
kagglehub>=0.3.0,<1.0  # 1.x (1.0.1) ships a broken kagglesdk import; pin to 0.3.x

# --- Config ---
python-dotenv>=1.0,<2.0

# --- Testing / tooling ---
pytest>=8.0,<9.0
httpx>=0.27,<1.0
ruff>=0.4,<1.0

# --- Optional providers (only used if configured) ---
# openai>=1.30
